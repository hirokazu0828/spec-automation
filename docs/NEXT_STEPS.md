# Layer 0 〜 Layer 3b-fix-step3-improvements 完了 → Phase B (マレット線画化) / Layer 3b-2 / Layer 5 / Layer 3c / Layer A への申し送り

> 直近完了レイヤ:
> - Layer 0 〜 Layer 2-PDF-perf: PR #1〜#9 → 統合ブランチ → PR #10 で `main` にマージ済
> - Layer 3a (型カタログ + アングル選択 UI): PR #11 → `main` にマージ済
> - Layer 3b (プロンプトテンプレート): PR #12 → `main` にマージ済
> - Layer 3b-fix-step3-improvements (4 アングル一括生成 + opt-in model param): `claude/layer3b-step3-improvements` (本 PR)

---

## Layer 0 → Layer 6 で解消したこと (要約)

- ✅ 値ずれバグ修正 / `SpecJson` 型拡充 / `colorA-D` デッドフィールド除去 / NG ルール一元化 *(Layer 0)*
- ✅ Step4 select / 寸法 / 形状語彙 / SampleBook フィルタが master 駆動 *(Layer 2)*
- ✅ Home に 3 ルート起点 (A/B/C)、`originRoute` メタ情報、Step1 OriginBadge *(Layer 6)*

## Layer 4 で実装したこと

### スキーマ追加
- ✅ `SpecData.documentType: 'sample' | 'final'` (新規 default 'sample'、旧ドラフトは 'final' に migrate)
- ✅ `SpecData.sampleRevision?: number` (sample 限定、`migrateSpecData` で `documentType !== 'sample'` のとき強制 undefined)
- ✅ `SpecData.sampleArrangement?: { quantities, unit, shippingDate?, arrangementNotes?, referenceSampleId? }`
- ✅ `SpecData.imageSource?: 'generated' | 'manual' | 'photo'`
- ✅ `DraftEnvelope` に `documentType` / `sampleRevision` をミラー (一覧表示の高速化)

### UI / フロー
- ✅ Home Route C カードに「サンプル指示書 / 最終仕様書」ラジオ + 「作成」ボタン
- ✅ Route A: SampleBook 詳細モーダルに docType ラジオ追加
- ✅ Route B: `DraftPickerModal` を行選択+確認方式に書き直し、「複製元と同じ」/「最終仕様書として作成」のラジオ (sample 起点限定)
- ✅ Step4 ヘッダーが docType 駆動 (「最終仕様書」/「SAMPLE指示書 1st」)、`ordinal()` ヘルパー追加
- ✅ Step4 PAGE 1 先頭に SAMPLE 手配欄 (sample のみ)
- ✅ DraftList に docType バッジ + 「次の rev」「最終確定」ボタン (sample 行のみ)
- ✅ `useSpecDrafts.promoteRevision(id)`: sample → 次 rev 複製、productCode サフィックス置換、改訂履歴に派生元行 prepend、画像コピー
- ✅ `useSpecDrafts.promoteToFinal(id)`: in-place で sample → final、imageSource は generated→manual に置換 (photo は温存)
- ✅ Step3 に「アップロード」ボタン + imageSource バッジ ('AI生成' / '手動アップロード' / '実物写真')、5MB 上限
- ✅ Step4 「並列出力モード」(secondary draft 概要を印刷で連結、最小実装)

判断記録: `docs/document-type-decisions.md` (docType 配置 / Route B 継承 / promoteToFinal の imageSource 規則 / 画像アップロード時の自動判定 等)

## Layer 6 UX fix で実装したこと

PR #5 で Route A のスマホ UX (起点ボタン到達性 + sticky footer) を直し、PR #6 で **Step2 到達後の認知負荷**を解消:

- ✅ Route A で Step2 マウント時に `generateProposals()` を自動発火 (5 提案カードが即時表示)
- ✅ Step2 ヘッダー直上に origin 別ガイドバナー (Route A: 「サンプル「{番号}」を起点に〜」、Route B: 「ドラフト「{productCode}」を複製〜」、Route C / undefined: 非表示)
- ✅ Step2 にも `draftLookup` prop を導入 (Step1 と同じパターン)
- ✅ React strict-mode の二重実行ガード (`autoGeneratedRef`)
- ✅ Route B / C / undefined では従来通り (バナー以外何も変わらない)

判断記録: `docs/route-a-step2-ux-decisions.md`

## Layer 6 UX fix-2 で実装したこと

タブナビ「常に許可」設計のため Step1 で position 未選択のまま Step2 へ飛んでしまう詰まりを解消:

- ✅ Step2 の `isPositionMissing` 派生状態 (`!data.position || data.position.trim() === ''`)
- ✅ amber 系の `<PositionMissingBanner>` (「ポジションが未設定です」+「STEP1 に戻る」ボタン、`min-h-[44px]`)
- ✅ position 未設定時は Route A/B の青系 `<GuideBanner>` より優先表示 (排他)
- ✅ Step2 に `onStepChange?: (step) => void` prop を追加、App.tsx の既存 `goStep` を渡してワンタップで Step1 復帰
- ✅ 全 originRoute (A/B/C/undefined) で同じ振る舞い
- ✅ Route A の auto-gen は元から `data.position` を gate にしていたので変更なし、テストで保証

判断記録: `docs/route-a-step2-position-fallback-decisions.md`

## Layer 2-PDF で実装したこと

- ✅ `@react-pdf/renderer` 4.x 導入 (React 19 サポート版)
- ✅ Step4 に「PDF を準備」ボタン → React.lazy で `PdfDownloadButton` 読込 → 「PDF をダウンロード」(`<PDFDownloadLink>`) で横向き A4 PDF 生成
- ✅ 単一モード 3 ページ / 並列モード 5 ページ (ドラフト別連続: p1=A+B / p2=A生地 / p3=A刺繍 / p4=B生地 / p5=B刺繍)
- ✅ 日本語フォント Noto Sans JP Regular + Bold WOFF を `@fontsource/noto-sans-jp` から postinstall で `public/fonts/` にコピー (`.gitignore` 済、リポサイズ膨張なし)
- ✅ `generatePdfFileName(data, secondary?)` でファイル名規則 `{productCode}_{type}{_revision}.pdf` (空時 `spec` フォールバック、並列時 `_vs_` 介入)
- ✅ 既存 `window.print()` 縦向きはフォールバックボタンとして並存
- ✅ Vitest は構造テスト (vi.mock で react-pdf primitives を div 化) で 9 ケース、PDF バイナリは生成しない
- ✅ メインバンドル 207KB に変化なし (PDF chunk = 1.4MB は遅延ロード、初回 Step4 表示は 0 影響)

判断記録: `docs/layer2-pdf-decisions.md`

## Layer 2-PDF-perf で実装したこと

PR #8 マージ後、Step4 の PDF 生成が実機で **~10〜12 秒**かかる問題を解消:

- ✅ `subset-font` (wasm hb-subset) + `joyo-kanji` を devDeps に追加
- ✅ `scripts/data/charset.ts` で `BUSINESS_CHARSET` (~2400 文字: ASCII + かな + 全角記号 + 常用漢字 2136 + 業務固有語) を定義
- ✅ `scripts/subset-fonts.ts` で フル WOFF (1380/1396 KB) → サブセット WOFF (451/457 KB、約 -67%) を出力
- ✅ `package.json` の `postinstall` を `download-fonts.ts && subset-fonts.ts` の連結に変更 (両方とも冪等)
- ✅ `src/components/Step4/pdf/fonts.ts` の `Font.register` を `-subset.woff` 版に切替
- ✅ `.gitignore` のコメントを subset 版にも対応するよう更新 (`public/fonts/` ディレクトリ単位の ignore は据え置き)
- ✅ Vitest 12 ケース追加 (`scripts/data/charset.test.ts` 6 + `scripts/subset-fonts.test.ts` 6)
- ✅ PDF 生成の体感: **~10〜12s → ~1〜2s** (目視ストップウォッチ計測)

判断記録: `docs/layer2-pdf-perf-decisions.md`

## Layer 3a で実装したこと

- ✅ `src/data/templates/catalog.json` を新設し、3 件の型 (blade / semi_mallet / full_mallet) を登録
- ✅ ブレード型は提供素材 (FOURTEEN TSUNOJI ベース、4 アングル分の PNG、LOGO プレースホルダー化) で線画完成 (`lineArtStatus: 'complete'`)
- ✅ セミマレット / フルマレットは枠だけ用意 (`lineArtStatus: 'pending_lineart'`、Phase B で線画化)
- ✅ Step3 を「型 select + 4 アングルタブ + 線図プレビュー」に再設計、type-shape ブリッジ (`getTemplateByHeadShape`) を追加
- ✅ `SpecData` に `templateId?` / `selectedAngle?` を追加 (optional、後方互換)
- ✅ Route A (SampleBook 起点) で headShape からカタログ ID を自動セット
- ✅ `buildImagePrompt(data, angle?)` に英語アングルフレーズを差し込み
- ✅ pending_lineart 時の UX: amber バナー + 前面のみ enable + 「ブレード型に切り替える」CTA + SVG フォールバック
- ✅ Vitest 20 件追加 (helpers 12 + Step3 UI 8) → 171 件全 pass

判断記録: `docs/layer3a-decisions.md`

## Layer 3b で実装したこと

シナリオ A (軽量リファクタ + position バリアント追加) を採択:

- ✅ `catalog.json` の各 template に **`promptShapeDescription`** + **`anglePromptPhrases`** を追加 (3 template × 2 fields)
- ✅ `buildImagePrompt.ts` の `SHAPE_DESC` ハードコード除去、catalog 駆動に統合
- ✅ `helpers.ts` に **`POSITION_PROMPT_PHRASES`** (luxury / standard / casual の 3 値、リスク回避ワード除去済) + **`getPositionPromptPhrase`** + **`getAnglePromptPhrase`** + **`NEGATIVE_PROMPT_PHRASE`** を追加
- ✅ `buildImagePrompt(data, angle?, position?)` に position 文を追加 (override-or-data パターン)
- ✅ `lining` 文を追加 (`Lining material is X.`、master JSON 駆動)
- ✅ ネガ風文末 (`Avoid text artifacts, plastic glare, or visible seams that break the silhouette.`) を常時 push
- ✅ `ANGLE_PROMPT_PHRASES` は global fallback として残置 (catalog 移管 + global fallback の 2 段構成)
- ✅ Vitest 26 件追加 (helpers +10 / buildImagePrompt +16) → **197 件全 pass** (171 → 197)
- ✅ プロンプトプレビュー UI (Step3 の薄青カード) は表示維持、Step3 呼び出し側は変更なし

判断記録: `docs/layer3b-decisions.md`、Phase B スコープは `docs/phase-b-decisions.md` に記録 (Layer 3b PR に同梱)。

## Layer 3b-fix-step3-improvements で実装したこと

Step3 の生成 UX を「1 枚ずつ → 4 アングル一括」に進化させた:

- ✅ `api/generate-image.ts` に opt-in `model` パラメータ (allowlist `['gpt-image-1','gpt-image-2']`、default は `'gpt-image-1'` で既存挙動完全維持)
- ✅ `generateImage.ts` に `generateImagesForAllAngles(buildOne, angles)` を新設 (`Promise.all` 並列発射 → 429 検出時のみ失敗アングルを順次 retry、500ms inter-call delay)
- ✅ `lib/imageStore.ts` に per-angle key (`<draftId>#<angle>`) サポートを追加。legacy `loadImage` / `saveImage` / `deleteImage` / `copyImage` は signature 不変 + 内部でレガシーキー fallback (Step4 / useSpecDrafts は無改修で動作)
- ✅ Step3 を 4 タイル grid に再設計 (一括生成ボタン + per-angle 再生成ボタン + 破棄ボタン + 生成済みアイコン + スピナー)
- ✅ pending_lineart テンプレでは bulk disabled、front タイルだけ再生成可
- ✅ コスト承認モーダルを `BULK_COST_LABEL = "約 25 円"` / `SINGLE_COST_LABEL = "約 6 円"` に更新 (gpt-image-1 medium 1024x1024 ベース)
- ✅ Step4 productPhotos へ insertToSheet on のとき front/side_toe/back を 正面/側面/背面 にマッピング、side_heel は IndexedDB のみで保持 (Step4 改修なし)
- ✅ 既存 Layer 3a Step3 テスト 8 件は無修正で全 pass、新規 12 件追加 → **209/209 pass**
- ✅ `scripts/run-step-b-test.cjs` は残置、header コメントで「現状 default は gpt-image-1、切替検証時に使用」を明記
- ✅ gpt-image-2 切替自体は保留 (将来再開手順を `docs/layer3b-step3-improvements-decisions.md §future-work` に記録)

判断記録: `docs/layer3b-step3-improvements-decisions.md`

---

## 次着手候補: Layer 3b-2 (部位別 + HEX + パイピング)

Layer 3b-fix-step3-improvements で見えてきた次の改善余地:

- **部位別の色指定** (catalog `parts: A〜F`) を gpt-image-1 プロンプトに反映する文型を追加
  - 例: `"The cut-and-sew section (B) on top should be in dark navy #1a2b4c, contrasting with the gray body."`
- **HEX カラー指定** をプロンプトに混ぜる経路 (`COLOR_HEX_MAP` の値を English 文型へ流す)
- **piping 文** の追加 (現状 `Pick` で受け取るがプロンプトに混ざっていない)
- closure 文の現状 (cord 系 4 値固定) は Layer A (master 拡張) で扱う

判断軸 / リスク回避フレーズの起案 → 承認フローは Layer 3b と同じ運用を踏襲予定。

---

## Phase B (Layer 3a-2) への申し送り

セミマレット / フルマレットの線画化。**Layer 3b 事前確認の中で系統混在問題が見つかったため、スコープは 2 型に絞り込み済**。詳細判断記録 → `docs/phase-b-decisions.md`

1. **線画ソース** (確定):
   - `putter-semi-mallet`: `BRG-2111-P1006` (HALF MALLET CS PUTTER COVER FIDLOCK, NAVY)
   - `putter-full-mallet`: `BRG-2004-P448` (MALLET PUTTER COVER FIDLOCK, GREEN, 系統 Y)
2. ChatGPT で 4 アングル (front / side_toe / back / side_heel) の線画化、1024×1024 PNG 化、LOGO プレースホルダー化
3. `public/templates/putter-{semi-mallet,full-mallet}/{front,side_toe,back,side_heel}.png` 配置
4. `catalog.json` の該当エントリで `baseImages` を埋めて `lineArtStatus` を `'complete'` に
   - 注: `anglePromptPhrases` と `promptShapeDescription` は **Layer 3b で先置き済**、線画配置と同時にすぐ使える
5. `metadata.source` / `createdAt` / `logoTreatment: 'placeholder'` を埋める
6. `sampleReferences` を `phase-b-decisions.md §4` のリストで更新
7. `helpers.test.ts` の `getCompleteTemplates` テストを 1 件 → 3 件に更新

### Phase B スコープ外: 系統 X 5 件 (`putter-full-mallet` だが形状はブレード寄り)

`BRG-2006-P541` / `BRG-2007-P571` / `BRG-2109-P897` / `BRG-2109-P936` / `BRG-2111-P1059` の 5 件は寸法 `W180×H150×D80mm` で形状的にブレード型に近い。Phase B では触らず、後続 Layer (仮称 **Layer 3a-fix-classification**) で再分類議論 (詳細 `docs/phase-b-decisions.md §5`)。

線画化スクリプトは Layer 3a-prep で `scripts/extract-template-lineart.ts` として整備予定 (現状はマニュアル運用)。

---

## Layer 5 (プロジェクトライフサイクル) への申し送り

### 着手前に確定したいこと

1. ライフサイクル状態 (draft / issued / approved / archived 等) の体系
2. 状態遷移を駆動するアクション (発行 / 承認 / 却下 / 再発行) と権限境界
3. 履歴・監査ログの保存先 (現状の自由テキスト `revisionHistory` を構造化するか、別ストアに記録するか)
4. PDF 発行履歴の保存 (`generatePdfFileName` で命名されたファイル群を IndexedDB に履歴保存するか / 都度生成し続けるか)

### Layer 5 着手時に活用できる Layer 0 〜 2-PDF の成果

- `documentType` / `sampleRevision` / `originRoute` がスキーマに揃っており、状態フラグを追加するだけでメタデータ層は揃う
- `migrateSpecData` のマイグレーション機構が確立 (旧ドラフトに新フィールドを補完するパターン)
- HTML プレビュー (Step4) と PDF 出力 (`SpecSheetPdf`) の重複を将来統合する場合、共通の中間表現を抽出するタイミングは Layer 5 が適切

---

## Layer 3 (image-2 連携 / 画像複数アングル / プロンプト改善) への申し送り

### 残課題

- imageSource の手動切替 UI (manual ↔ photo の override) — Layer 4 では documentType ベースの自動判定のみ
- 画像複数アングル対応 (現状は 1 ドラフト = 1 画像)
- image-2 のプロンプト改善 (Layer 4 では `buildImagePrompt` を変更せず触っていない)
- 線図抽出スクリプト `scripts/extract-lineart.ts` の実用化 — 現状 `/public/images/` がサンプルシートのため出力が荒れる

---

## Layer A (ストレージ分離 + マスタ編集 UI) への申し送り

`docs/QUESTIONS.md §A` 4 つの意思決定が前提。Layer 0 〜 4 で master 参照点が `src/utils/specHelpers.ts` (`getLabel` / `getOptions` / `getDimensionDefault` / `getShapeByAlias` 等) と `src/utils/ngRules.ts` に集約済 → 関数の入口を JSON import から fetch / DB 経由に差し替える形での移行が容易な状態。

---

## 触らないと約束したファイル / 領域 (全 Layer 共通スコープ外)

- `api/generate-image.ts`
- `src/components/Step3/generateImage.ts` の OpenAI 呼出周り
- `src/lib/imageStore.ts` (IndexedDB 保存形式)
- 既存 Step UI の根本的な書き換え

---

## Layer 2-PDF-perf PR 時点の動作確認

- `npm run lint`: 0 エラー
- `npm test`: **151 件全 pass** (139 件 + Layer 2-PDF-perf で +12: charset 6 + subset-fonts 6)
- `npm run build`: 型エラー 0
- `npm run download:fonts && npm run subset-fonts`: 冪等 (2 回目以降 skip)、`/public/fonts/` に 4 ファイル (フル + subset × Regular/Bold)

### Desktop 側で要再確認のポイント

1. **Route C で「サンプル指示書」を選んで作成** → Step4 ヘッダーが「SAMPLE指示書 1st」(橙バナー) で表示
2. **Route C で「最終仕様書」を選んで作成** → Step4 ヘッダーが「最終仕様書」(赤バナー) で表示
3. サンプル指示書の Step4 PAGE 1 先頭に **SAMPLE 手配欄** が表示される (出荷納期 / 客人用・東京用・工場用 数量 / 単位 / 備考 / 参考サンプル)
4. 最終仕様書の Step4 PAGE 1 先頭に SAMPLE 手配欄が**表示されない**
5. ドラフト一覧で documentType バッジ ("最終仕様書" / "SAMPLE 1st" 等) が正しく表示
6. sample 行で「次の rev」ボタン → sampleRevision +1、productCode が `_2nd` 付きで複製、改訂履歴の先頭に派生元行が入る
7. sample 行で「最終確定」ボタン → 確認ダイアログ後、その行が緑「最終仕様書」バッジに変わる
8. Step3 で「アップロード」ボタン → ファイル選択 → imageSource バッジが「手動アップロード」(sample 時) または「実物写真」(final 時) に
9. Step4 ヘッダーの「並列出力モード」チェック → 別ドラフトを選ぶと、画面下部 + 印刷出力に secondary draft の概要 (read-only) が現れる
10. 旧ドラフト (Layer 4 以前に作成) を開いてもバッジが「最終仕様書」表示で開けて、SAMPLE 手配欄は出ない

### 既知の制約 / Layer 4 スコープ外 (持ち越し)

- 画像アップロード時の imageSource 切替 UI (manual ↔ photo) は documentType 自動判定のみ → Layer 3
- 並列出力は概要連結のみ (フル 3 ページ並列 / 横向きレイアウト最適化なし) → Layer 2-PDF / Layer 5
- 状態管理 (draft / issued / approved) は未実装 (Q1-B 未決)
- 7 件の既存仕様書 PDF 実データ確認 — Web sandbox からアクセス不可、Desktop 側で実施
