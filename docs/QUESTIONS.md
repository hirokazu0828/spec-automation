# 設計者への確認事項

> `docs/CURRENT_STATE.md` 調査中に判断保留した点を整理。
> 末尾に「次アクション提案 (3〜5案)」を優先度付きで記載。

---

## A. マスタデータ (Layer 2 着手前提)

### A-1. マスタ DB 分離の到達点はどこ?

現状: `src/data/spec/putter-cover.json` 1 ファイルが React バンドルに同梱され、コードは `import { specJson } from '../../data/spec'` で**単一ドメイン固定参照**。`domains` レコードと `DomainKey` 型は宣言されているが切替 UI/state なし。

確認したい:

- [ ] **目指すゴール**: 単に「複数ドメイン (パターカバー以外) に切り替える state を持つ」ことか、「**ファイルを編集せずマスタを書き換えられる**」ことか
  - 後者の場合、保存先候補は (a) ブラウザ localStorage、(b) Vercel KV / Supabase などのリモート DB、(c) GitHub PR ベース (JSON を Git で管理)、(d) 管理画面付き
- [ ] **マスタの編集権限**: 全ユーザー / 管理者のみ / 開発者のみ
- [ ] **バージョニング**: マスタが更新されたとき、過去仕様書 (drafts) が参照する値はどう扱うか (固定スナップショットか、追従か)
- [ ] **ドメイン追加**: パターカバー以外にどの商品ドメインを追加する想定か (ヘッドカバー / キャディバッグ / グローブ 等)。各ドメインは parameters の **形** が異なるか (動的フォーム生成が必要か)

### A-2. マスタとコードの不整合をどう扱うか

`CURRENT_STATE.md §6` で列挙した:

- (1) `parameters.*.ng_rules` JSON 定義 vs Step2 ハードコード — Layer 2 で **JSON 駆動** にするか
- (2) Step4 PAGE 3 の刺繍技法 `<select>` ハードコード (マスタに対応する英語 ID あり) — マスタ参照に置き換えるか
- (3) `useStep2Proposals` 内の `pu_shibo` / `silver_matte` (マスタにない値) — **既存バグの修正**として一括で直していいか
- (4) サンプル帳のフィルタ語彙 (`マグネット` `ベルクロ` 等) と spec マスタ (`wire_spring` 等) の語彙の橋渡し — 翻訳辞書を作るか / マスタを増補するか / フィルタを samples.json から動的算出するか

---

## B. 出力フォーマット (Layer 4 サンプル指示書テンプレ)

### B-1. 「サンプル指示書」と「最終仕様書」は何が違うか

現状: Step4 = 最終仕様書 (3 ページ A4 縦) のみ。

確認したい:

- [ ] サンプル指示書のページ数 / 載せる情報の差分 (例えば「指示書はサンプル番号 + 簡易仕様 1 ページ」「最終仕様書は 3 ページ」など)
- [ ] **共通データ vs テンプレ固有データ**: SpecData は両者で共通か、テンプレ固有フィールドが要るか (例: 指示書だけ「依頼日」「希望納期」)
- [ ] テンプレ切替の UI 場所: ホーム新規作成時に選ぶ / Step1 で選ぶ / Step4 でトグル
- [ ] 出力先: PDF ダウンロード / ブラウザ印刷 / メール送信 / Box アップロード

### B-2. A4 横向き要件はどのテンプレで必要か

現状: 縦のみ。

- [ ] 横向きが必要なのは: 指示書 / 最終仕様書 / 両方 / オプション
- [ ] 横向きで載せたい追加情報: 線図 / 大型画像 / 表の列が増える 等

---

## C. プロジェクトライフサイクル (Layer 5)

### C-1. リビジョン管理の粒度

現状: `data.revisionHistory: Array<{date, content}>` の自由入力テキストのみ。スナップショット / バージョン番号なし。

- [ ] 求める機能: (a) 過去版を **見るだけ** (b) **比較** (c) **ロールバック** (d) **承認フロー** (起票 → レビュー → 承認 → 工場へ)
- [ ] バージョン採番: 手動 / 自動 (v1.0 / v1.1 / v2.0)
- [ ] 各リビジョンが**独立した PDF ファイル**として残るか、システム内で履歴管理するか

### C-2. 「ドラフト」の状態遷移

現状: 単一 status (drafts 配列に入っているかどうかのみ)。

- [ ] status の候補: ドラフト / レビュー中 / 承認済 / 出荷済 / アーカイブ
- [ ] 状態遷移を誰が引くか (担当者本人 / 上長 / 自動)

### C-3. ロックとマルチユーザー

現状: 同一 ID のドラフトを別タブで開くと最後に保存した方が勝つ。`storage` event でクロスタブ同期するが衝突時の保全はない。

- [ ] 同時編集の想定: あり / なし
- [ ] 保存先: localStorage 続行 / リモート DB 化 (= マルチデバイス対応)

---

## D. 3 ルート起点判定 (Layer 6)

設計上の理解が浅いため、**まず仕様自体を共有してほしい**。

- [ ] **A/B/C ルート** とはそれぞれ何か (例: A=過去サンプル踏襲、B=新規企画、C=部分流用 ?)
- [ ] **判定の入力**: ユーザーが選ぶ / システムが推測 / 両方
- [ ] **判定タイミング**: ホームで新規作成時 / Step1 / 任意のとき
- [ ] **判定後の分岐**: ルートごとに通る Step が違うか / 同じ Step で挙動が違うか / 出力フォーマットが違うか
- [ ] 既存資産との関係: SampleBook (= 過去サンプル) との接続、useStep2Proposals (= AI 提案) との関係

---

## E. AI / 画像連携 (Layer 3 image-2)

### E-1. 「image-2 連携」とは具体的に何か

候補:
- (a) Step3 の生成画像をクラウドストレージに上げる
- (b) Step3 の前段に「参考画像アップロード → それを線図化 → 編集」フローを追加
- (c) 別の画像生成パイプラインに切替 (Stable Diffusion / Midjourney 復活 / Imagen)
- (d) 既存の `/api/generate-image.ts` (gpt-image-1 image-edit) を Sora / GPT-5-image 等に差し替え

確認したい:
- [ ] 上記どれに近いか / 別の意図か
- [ ] サンプル写真 → 線図抽出 (`scripts/extract-lineart.ts`) を再活用する案件はあるか (現状 `/public/images/` は合成シートで使えない、単品写真調達の予定はあるか)

### E-2. 提案ロジック (`useStep2Proposals`) の AI 化

現状: ローカル乱数 + テンプレートのみで OpenAI/Anthropic 接続なし。

- [ ] LLM API による提案生成への置換は本ロードマップに含むか
- [ ] 含む場合、プロンプトのソース (過去仕様書 = drafts、サンプル帳 = samples.json、マスタ = putter-cover.json) のどれを文脈に入れるか

---

## F. 既存実装の小さな疑問

- [ ] `SpecData.colorA / colorB / colorC / colorD` (デッドフィールド) — 削除して OK か
- [ ] Step4 の `sewingNotes` が PAGE 2 と PAGE 3 で同じ field を共有している (両方とも `data.sewingNotes`) のは意図的か / 分けるべきか
- [ ] AuthGate がアプリ全体を覆っているのは意図的か / サンプル帳のみに戻すか (前回再設計で全体ラップに変更した経緯あり)
- [ ] `useStep2Proposals` の `pu_shibo` / `silver_matte` (= マスタ未定義値) は `pu_lizard` / `matte_silver` のタイポか
- [ ] `combinations_count` (putter-cover.json) — 11,088,000 という巨大数値は表示用か、計算検証用か (現状コードでは未参照)

---

## 私への次アクション提案 (優先度順)

実装に入る前に、まず**Layer 2 = マスタ DB 分離**の方針を確定したいので、優先度順に 5 案。

### 🔴 優先度 高

1. **A-1, A-2 を確定する 30 分セッション**
   `docs/QUESTIONS.md` の §A を上から答える形で OK。マスタの最終的な保存先 (localStorage / リモート DB / GitHub) と、コード側のハードコード重複 (NG ルール、刺繍 select、`pu_shibo` バグ) を「Layer 2 の中で直す / 後回し」に振り分けたい。
   - **アウトプット**: `docs/LAYER2_PLAN.md` (実装計画)。

2. **D を確定する 30 分セッション**
   3 ルート起点判定 (A/B/C) の仕様共有。Layer 2 の段階で「マスタの中にルート種別フィールドを持たせるべきか」が決まらないと、後で破壊的変更になるため**Layer 2 着手前に把握必須**。

### 🟡 優先度 中

3. **B-1, C-1 を要件レベルでスケッチ**
   サンプル指示書 vs 最終仕様書、リビジョン管理の到達点をテキストで 5〜10 行ずつ。「最低限こうあってほしい」レベルで OK。Layer 2 のスキーマ設計 (= テンプレ種別フィールド、version フィールド) に影響する。

4. **既存バグの一斉修正 PR を独立で出す**
   `useStep2Proposals` の `pu_shibo` / `silver_matte` 値ずれ + Step4 刺繍技法の JSON 化 (重複削除)。Layer 2 と独立して着手可能、影響範囲が小さく保守性が上がる。
   - **見積もり**: 0.5 日。テスト追加込み。

### 🟢 優先度 低 (Layer 2 着手後でいい)

5. **テスト整備 (Step1/Step3/Step4 と useStep2Proposals)**
   現状 26 件、`Home`/`useStep2Proposals`/`generateImage`/`SampleBook` のカバレッジゼロ。Layer 2 でマスタ操作 UI を作るときに足元が緩いと厳しいので、**Layer 2 のスキーマ確定後**に着手するのが効率的。
   - **見積もり**: 1〜1.5 日。

---

## B. 画像生成 API のセキュリティ (Layer 3b で記録、後続 Layer で対処)

### B-1. `/api/generate-image` が無認証で叩ける

**観測**: `api/generate-image.ts` (Vercel Function) は **`req.method === 'POST'` だけをチェック**して `process.env.OPENAI_API_KEY` で OpenAI に転送する。フロント側で `window.confirm()` の確認モーダルは出るが、**サーバ側で認証はしていない** (`api/generate-image.ts:11-18`)。

**リスク**:
- アプリの URL を知っている誰もが `curl` で `/api/generate-image` を直接叩ける
- レートリミットなし → OpenAI 課金が短時間で膨れ上がるリスク
- アプリ全体は `AuthGate` (`VITE_APP_PASSWORD`) で守られているが、API 側はその防御を共有していない (`AuthGate` は **クライアント側 sessionStorage 認証** で API には到達しない)

**スコープ判断**: Layer 3b はプロンプトテンプレートの整備が主目的。API 認証は別 Layer で扱う。

**後続 Layer 案**:
- フロントが既に持つ `VITE_APP_PASSWORD` を `/api/generate-image` 呼び出し時の `Authorization: Bearer <password>` ヘッダに乗せ、サーバ側で照合する最小実装
- もしくは Vercel の Edge Middleware で IP / Cookie ベースのレート制限を追加
- 完全な認証 (Auth0 / Vercel Auth など) は業務環境の認証基盤決定後

**Layer 3b では: 何もしない**。本 Issue は記録のみ。
